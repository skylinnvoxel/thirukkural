# Optional: URL to a hosted JSON file (or API) with the complete, verified
# 1,330-Kural dataset, matching the Kural[] shape in src/data/types.ts.
# When set, it is merged on top of the bundled sample dataset at startup.
# Leave unset to run with the small bundled sample (Kurals 1–10).
VITE_KURAL_DATA_URL=
